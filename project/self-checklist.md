# Чек-лист самопроверки проекта

## 1. Постановка задачи и данные
- [x] Описание задачи, предметной области и пользователей в README.md и report.md
- [x] Сформулированы цели и метрики (mIoU, Precision, Recall)
- [x] Зафиксированы ограничения и риски (качество данных, дисбаланс, объём, задержки)
- [x] Настроен пайплайн загрузки данных с Hugging Face
- [x] Данные разбиты на Train / Val / Test (70/20/10)
- [x] Проведен первичный анализ данных (EDA) — ноутбук `notebooks/01_eda.ipynb`
- [x] Визуализации: распределение классов, размеры изображений, сложность полигонов, примеры аннотаций

## 2. Модели и эксперименты
- [x] Реализована современная архитектура (Mask R-CNN ResNet-50 FPN V2)
- [x] Реализованы классические/облегчённые бейзлайны:
 - [x] HSV CV Baseline (традиционная CV-сегментация по цвету)
 - [x] Mask R-CNN MobileNetV3 (облегчённый вариант)
 - [x] FCN ResNet-50 (базовая семантическая сегментация)
- [x] Проведено сравнение моделей (скрипт `compare_models.py`, отчёт в `outputs/model_comparison/`)
- [x] Обоснован выбор финальной модели (ResNet-50 — лучшее качество)
- [x] Настроены аугментации данных (HorizontalFlip, RandomBrightnessContrast)
- [x] Применена регуляризация (weight decay)
- [x] Настроен подбор гиперпараметров (Optuna — скрипт `tune_hyperparameters.py`)
- [x] Зафиксировано, какие изменения улучшили показатели (report.md, раздел 4)

## 3. Эксперименты и трекинг
- [x] Настроен трекинг экспериментов через MLflow
- [x] Логируются: параметры, метрики по эпохам, test-метрики, время инференса, артефакты
- [x] Версионируются данные (seed, сплиты) и конфигурации
- [x] Реализован режим дообучения (resume)
- [x] История обучения сохраняется в history.json

## 4. Сервисная часть
- [x] Реализован REST API на FastAPI (`main.py`)
- [x] Эндпоинты: `/health`, `/predict`, `/metrics`
- [x] Описаны входные/выходные данные, статус-коды (200, 400, 422, 500, 503)
- [x] Реализован health-check эндпоинт
- [x] Реализованы Prometheus-метрики (request count, duration, predict count, model loaded)
- [x] Настроено структурированное логирование (модуль `logging`, формат: timestamp | level | module | message)
- [x] Создан интерактивный UI на Streamlit (`app.py`)

## 5. Инфраструктура и безопасность
- [x] Проект упакован в Docker (`Dockerfile`)
- [x] Используется docker-compose для запуска API и UI
- [x] Настройки вынесены в конфиги (`configs/config.yaml`) и `.env.example`
- [x] Секреты не попадают в репозиторий — `.gitignore` в корне проекта
- [x] `.gitignore` покрывает: `.env`, `__pycache__`, `outputs/*.pth`, `data/raw/`, `mlruns/`

## 6. Тестирование
- [x] API-тесты в `tests/test_api.py` (health-check, predict, метрики, smoke, граничные случаи)
- [x] Модульные тесты в `tests/test_modules.py` (data, metrics, modeling, inference, baselines, utils)
- [x] Smoke-тесты: последовательные запросы к API
- [x] Покрыты: утилиты, парсинг аннотаций, метрики, модель, HSV-детектор, инференс

## 7. Документация
- [x] `README.md` — описание архитектуры, быстрый старт, Docker, тестирование
- [x] `report.md` — полный отчёт по всем этапам проекта
- [x] `self-checklist.md` — данный чек-лист
- [x] `notebooks/01_eda.ipynb` — ноутбук с EDA
- [x] Сгенерированы отчёты о качестве модели (`generate_report.py`)
- [x] Подготовлена дорожная карта развития (Roadmap в README.md)

## 8. Дополнительно
- [x] Структура проекта соответствует шаблону: `src/`, `data/`, `configs/`, `notebooks/`, `tests/`, `outputs/`
- [x] Все скрипты имеют аргументы командной строки (argparse)
- [x] Поддерживается воспроизводимость (set_seed, фиксированные сплиты)
- [x] Код аннотирован типами (type hints) и документирован (docstrings)
